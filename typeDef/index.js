import shared from "./shared";
import restaurant from "./restaurant";
import room from "./room";
import root from "./root";

const schemaArray = [root, shared, room, restaurant];

export default schemaArray;
